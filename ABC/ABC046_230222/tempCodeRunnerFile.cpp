
  // cin >> N;
  // vector<int> T(N),A(N);
  // for(int i = 0;i<N;i++){
  //   cin >> T[i] >> A[i];
  // }

  // vector<long> t(N),a(N);
  // long n;
  // t[0] = T[0];
  // a[0] = A[0];
  // for(int j=1;j<N;j++){
  //   n = max(floor((double) t[j-1]/T[j]),floor((double) a[j-1]/A[j]));
  //   // cout << n << endl;
  //   t[j] = n*T[j];
  //   a[j] = n*A[j];
  //   // cout << t[j]+a[j] << endl;
  // }