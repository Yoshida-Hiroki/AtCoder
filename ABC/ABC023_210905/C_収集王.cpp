#include <bits/stdc++.h>

using namespace std;

int main(){
  int R,C,K,N;
  cin >> R >> C >> K;
  cin >> N;

  int r,c;
  vector<int> R(N),C(N);
  for(int i = 0;i<N;i++){
    cin >> r.at(i) >> c.at(i);
    R.at(r)++;
    C.at(c)++;
  }



}
